import styled from 'styled-components';

const Container = styled.div`
  display: flex;
  align-items: center;

  .slick-list {
    width: 850px;
    height: 310px;
  }
`;

export default { Container };
